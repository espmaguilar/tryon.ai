[project]
name = "my-agent"
version = "0.1.0"
description = "Vision Agents voice assistant example"
requires-python = ">=3.12,<4.0"
dependencies = [
    "vision-agents[decart,gemini,getstream]",
    "python-dotenv",
]

[project.urls]
"Homepage" = "https://visionagents.ai"
